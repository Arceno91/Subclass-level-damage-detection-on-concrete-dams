# 复现包索引（一页纸）

本包是论文的归档材料，配合 GitHub 代码包（`damcrack-sub5-code`）使用。

## 我想干什么 → 去哪里

| 目的 | 去哪 |
|---|---|
| 查论文某个数字的出处 | `results_json/`（内含 文件→论文位置 对照表） |
| 拿标注做自己的实验 | `derived_labels/`（不含图像，图像按 `LICENSE_DATA.md` 下载） |
| 复现训练/评估 | `scripts_damsegment/`（脚本）+ `framework/`（框架改动）+ `configs/`（配置） |
| 看框架改了什么 | `framework/改动说明.md`（中文逐处说明）+ `changes.patch` |
| 找训练好的权重 | `weights_and_samples/weights/`（基线 3 种子、YOLO26 结构、K-shot 等） |
| 看数据集统计/来源 | `data_manifest/` |

## 目录一览

```
derived_labels/      标注（含骨架伪标签）
results_json/        28 个结果文件
scripts_damsegment/  全部实验脚本（data/detection/fewshot/inference/report）
framework/           7 个框架文件 + 改动说明 + patch
configs/             数据集描述 + 模型定义
weights_and_samples/ 权重与样例
data_manifest/       数据集清单与统计
LICENSE_DATA.md      数据来源与许可
```

## 上传 Zenodo 前要做的

1. 填 `README.md` 末尾的联系人/仓库地址（现在是 AUTHOR_INPUT_NEEDED 占位）；
2. 用 `reproducibility_package_20260923.zip` 上传（解包后即为本目录结构）；
3. 拿到 DOI 后回填论文 Data availability 与 GitHub 包 README。
